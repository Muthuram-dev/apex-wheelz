<!DOCTYPE html>
<html>
<head>
  <title>Edit Order</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="container mt-4">
  <h2>Edit Order</h2>
  <form method="POST" action="/edit/<%= order.id %>" class="row g-3">
    <input class="form-control" name="product_name" value="<%= order.product_name %>" required>
    <input class="form-control" name="product_desc" value="<%= order.product_desc %>">
    <input class="form-control" name="customer_name" value="<%= order.customer_name %>" required>
    <input class="form-control" name="customer_email" value="<%= order.customer_email %>">
    <input class="form-control" name="customer_phone" value="<%= order.customer_phone %>">
    <input class="form-control" type="date" name="order_date" value="<%= order.order_date %>" required>
    <input class="form-control" name="status" value="<%= order.status %>">
    <button type="submit" class="btn btn-primary">Update</button>
  </form>
  <a href="/records" class="btn btn-link mt-3">Back</a>
</body>
</html>
